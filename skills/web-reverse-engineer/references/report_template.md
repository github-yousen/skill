# {网站名} 操作手册

> 首次分析时间：{TIMESTAMP}
> 目标网站：{TARGET_URL}
> 文档用途：**下次直接看本文档操作，无需重新分析**

---

## 一、基本信息

| 项目 | 内容 |
|------|------|
| 目标 URL | {TARGET_URL} |
| 前端框架 | {FRAMEWORK} （Vue / React / Angular / Next.js / Nuxt.js） |
| 打包工具 | {BUNDLER} （Vite / Webpack） |
| 状态管理 | {STATE_MANAGEMENT} （Pinia / Vuex / Redux） |
| 渲染方式 | {RENDER_MODE} （CSR / SSR / CSR+SPA） |
| Source Map | {HAS_SOURCEMAP} （有 / 无，如有则可获取完整源码） |
| Service Worker | {HAS_SW} （有 / 无） |
| 主 JS 文件 | {MAIN_JS_URL} |
| JS 文件数 | {JS_COUNT} （含 chunk） |

---

## 二、凭证说明

> 操作本网站需要提供以下凭证

| 凭证名 | 获取方式 | 用途 | 必须？ |
|--------|----------|------|--------|
| `{凭证1}` | 登录后从 Cookie 获取 | 身份认证 | ✅ |
| `{凭证2}` | 登录后从 Cookie 获取 | CSRF Token，POST 必带 | ✅ |
| `{凭证3}` | 登录后从 Cookie 获取 | 设备标识 | 可选 |

**如何获取 Cookie**：
1. 浏览器登录网站
2. F12 → Application → Cookies → 找到对应域名
3. 复制所需 Cookie 值

**鉴权方式**：`{COOKIE / BEARER_TOKEN / API_KEY / OAUTH2}`

**签名机制**：`{参数签名 / HMAC-SHA256 / MD5 / WBI / 无}`

---

## ★ 三、可直接执行的操作

> 这是核心章节。给我凭证，我可以直接完成以下任何操作。

### 3.1 {功能模块1，如：内容/视频}

| # | 操作 | 方法 | 接口 | 必要参数 | 说明 |
|---|------|------|------|----------|------|
| 1 | 获取推荐列表 | GET | `/api/xxx/feed` | 无 | 需携带 Cookie |
| 2 | 搜索内容 | GET | `/api/xxx/search` | `keyword, page` | 可能需要签名 |
| 3 | {操作3} | POST | `/api/xxx/add` | `id, csrf` | 需 CSRF Token |

### 3.2 {功能模块2，如：用户/账户}

| # | 操作 | 方法 | 接口 | 必要参数 | 说明 |
|---|------|------|------|----------|------|
| 1 | 获取用户信息 | GET | `/api/user/info` | 无 | |
| 2 | {操作2} | POST | `/api/xxx` | `csrf` | |

### 3.3 {功能模块3}

| # | 操作 | 方法 | 接口 | 必要参数 | 说明 |
|---|------|------|------|----------|------|

---

## 四、鉴权与签名

### 4.1 标准请求头

```
Cookie: {cookie_name}={value}; {csrf_name}={csrf_value}
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36
Referer: {TARGET_URL}
```

### 4.2 签名算法（如有）

**类型**：`{SIGN_TYPE}` （参数签名 / HMAC-SHA256 / MD5 / WBI / 无）

```python
# 签名算法代码（逆向后粘贴此处）
```

**需要签名的接口**：
- `{path1}`
- `{path2}`

### 4.3 多步操作依赖链

某些操作需要先后调多个接口：

```
步骤1: {接口A} → 获取 {数据X}
步骤2: 用 {数据X} 做 {处理Y}
步骤3: {接口B}（带 {处理Y} 的结果） → 完成操作
```

### 4.4 WebSocket 鉴权（如有）

| 端点 | 鉴权方式 | 说明 |
|------|----------|------|
| `{ws_url}` | URL参数传token | 连接时携带 `?token=xxx` |

---

## 五、完整 API 接口清单

> 按模块分组，包含从 JS 源码中提取的所有接口

### 5.1 认证 / 用户

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|
| GET | `{path}` | | 获取用户信息 |
| POST | `{path}` | | 登录 |
| GET | `{path}` | | 登出 |

### 5.2 {业务模块}

| 方法 | 路径 | 参数 | 说明 |
|------|------|------|------|

### 5.3 GraphQL（如有）

| 操作类型 | 操作名 | 参数 | 说明 |
|----------|--------|------|------|
| query | `{operationName}` | `{variables}` | {描述} |
| mutation | `{operationName}` | `{variables}` | {描述} |

### 5.4 WebSocket（如有）

| 端点 | 用途 | 鉴权 |
|------|------|------|
| `{ws_url}` | {描述} | {方式} |

### 5.5 上报/监控（通常忽略）

| 方法 | 路径 | 说明 |
|------|------|------|

---

## 六、页面功能地图

| 路径 | 功能 | 需要登录 | 对应主要接口 |
|------|------|---------|-------------|
| `/` | 首页 | 否 | `GET /api/feed` |
| `/login` | 登录 | 否 | `POST /api/login` |
| `/{user}` | 用户主页 | 否 | `GET /api/user/{id}` |
| `/settings` | 设置 | ✅ | `GET /api/settings` |

---

## 七、附录

### 7.1 域名体系

| 域名 | 用途 |
|------|------|
| `{domain}` | 主站 |
| `api.{domain}` | 主 API |
| `passport.{domain}` | 鉴权/登录 |

### 7.2 JS 文件清单

| 文件名 | 大小 | API数 | 用途 |
|--------|------|-------|------|
| `index-{hash}.js` | {size} | {count} | 主入口 |

### 7.3 Source Map 可用性

| 文件 | 状态 | 说明 |
|------|------|------|
| `{map_file}` | 已下载 | 可还原完整源码 |

### 7.4 未覆盖的功能

- [ ] {功能1}（原因：需要特殊权限/未深入分析）
- [ ] {功能2}

### 7.5 分析环境与局限

| 项目 | 说明 |
|------|------|
| 请求失败数 | {FAIL_COUNT} / {TOTAL} |
| 被限流 | 是/否 |
| 未获取的 JS | {列表} |
| SPA 渲染内容 | 未获取（需浏览器自动化） |

---

*文档由 web-reverse-engineer 技能自动生成，如接口有变化请重新分析更新。*
